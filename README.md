# Enterprise RAG AI Assistant

> A **production-ready** Retrieval-Augmented Generation (RAG) AI Assistant built with
> FastAPI, Next.js 15, and Docker — designed for enterprise document Q&A at scale.

[![Python](https://img.shields.io/badge/Python-3.12-blue?logo=python)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.115-green?logo=fastapi)](https://fastapi.tiangolo.com/)
[![Next.js](https://img.shields.io/badge/Next.js-15-black?logo=next.js)](https://nextjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5-blue?logo=typescript)](https://www.typescriptlang.org/)
[![Docker](https://img.shields.io/badge/Docker-Compose-blue?logo=docker)](https://docs.docker.com/compose/)

---

## Table of Contents

- [Overview](#overview)
- [Folder Structure](#folder-structure)
- [Prerequisites](#prerequisites)
- [Quick Start — Local Development](#quick-start--local-development)
- [Running with Docker](#running-with-docker)
- [API Reference](#api-reference)
- [Environment Variables](#environment-variables)
- [Running Tests](#running-tests)
- [Future Roadmap](#future-roadmap)

---

## Overview

The Enterprise RAG AI Assistant allows enterprise users to:

1. **Upload documents** (PDF, DOCX, TXT) to a knowledge base.
2. **Ask questions** in natural language against that knowledge base.
3. **Receive grounded, cited answers** generated by a large language model.

**Phase 1 (current)** establishes the production-grade project foundation:
clean architecture, typed configuration, structured logging, Docker infrastructure,
and a versioned API scaffold.

---

## Folder Structure

```
enterprise-rag-ai-assistant/
│
├── backend/                        # Python / FastAPI backend
│   ├── app/
│   │   ├── api/
│   │   │   └── v1/
│   │   │       ├── router.py       # Aggregates all v1 sub-routers
│   │   │       └── endpoints/
│   │   │           ├── health.py   # GET /api/v1/health
│   │   │           └── root.py     # GET /api/v1/
│   │   ├── config/
│   │   │   ├── settings.py         # Pydantic-settings (all env vars)
│   │   │   └── config.py           # Constants & feature flags
│   │   ├── core/
│   │   │   ├── logging.py          # Loguru configuration
│   │   │   └── exceptions.py       # Custom exception hierarchy
│   │   ├── middleware/
│   │   │   ├── cors.py             # CORS policy
│   │   │   └── logging_middleware.py  # Request/response logging
│   │   ├── schemas/
│   │   │   └── common.py           # Shared Pydantic response models
│   │   ├── models/                 # ORM models (Phase 3)
│   │   ├── services/               # Business logic (Phase 4)
│   │   ├── utils/                  # Shared utilities
│   │   ├── dependencies/           # FastAPI DI registry
│   │   └── main.py                 # Application factory + lifespan
│   ├── tests/
│   │   └── test_health.py          # Endpoint tests
│   ├── .env.example                # Environment variable template
│   ├── Dockerfile                  # Multi-stage Python 3.12 image
│   └── requirements.txt            # Pinned Python dependencies
│
├── frontend/                       # Next.js 15 + TypeScript + Tailwind
│   └── src/app/
│       ├── layout.tsx              # Root layout + metadata
│       └── page.tsx                # Homepage
│
├── docker/
│   └── nginx.conf                  # Nginx reverse proxy (Phase 5)
│
├── docs/
│   └── architecture.md             # ADRs + component diagrams
│
├── docker-compose.yml              # Backend + frontend services
├── .gitignore
└── README.md
```

---

## Prerequisites

| Tool | Minimum Version | Install |
|------|----------------|---------|
| Python | 3.12 | [python.org](https://python.org) |
| Node.js | 20 LTS | [nodejs.org](https://nodejs.org) |
| npm | 10 | Bundled with Node.js |
| Docker Desktop | Latest | [docker.com](https://docker.com) |
| Git | 2.x | [git-scm.com](https://git-scm.com) |

---

## Quick Start — Local Development

### 1. Clone the Repository

```bash
git clone <your-repo-url>
cd enterprise-rag-ai-assistant
```

### 2. Backend Setup

```bash
# Navigate to the backend directory
cd backend

# Create a Python virtual environment
python -m venv .venv

# Activate the virtual environment
# On Windows:
.venv\Scripts\activate
# On macOS/Linux:
source .venv/bin/activate

# Install dependencies
pip install -r requirements.txt

# Create your local .env file from the template
cp .env.example .env

# Start the development server (with hot-reload)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

The API is now available at:
- **API Root**: http://localhost:8000/api/v1/
- **Health Check**: http://localhost:8000/api/v1/health
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 3. Frontend Setup

Open a **new terminal**:

```bash
# Navigate to the frontend directory
cd frontend

# Install Node.js dependencies
npm install

# Start the development server
npm run dev
```

The frontend is now available at: **http://localhost:3000**

---

## Running with Docker

Docker Compose runs the entire stack (backend + frontend) with a single command.

### Start All Services

```bash
# Build images and start all services in the foreground
docker-compose up --build

# Or run in detached (background) mode
docker-compose up --build -d
```

### Verify Services Are Healthy

```bash
# Check container status
docker-compose ps

# Check backend health
curl http://localhost:8000/api/v1/health

# Stream backend logs
docker-compose logs -f backend

# Stream frontend logs
docker-compose logs -f frontend
```

### Stop All Services

```bash
docker-compose down

# Also remove volumes (clears logs)
docker-compose down -v
```

---

## API Reference

All API endpoints are prefixed with `/api/v1/`.

### `GET /api/v1/`

Returns a welcome message confirming the API is reachable.

**Response `200 OK`:**
```json
{
  "message": "Enterprise RAG AI Assistant API"
}
```

---

### `GET /api/v1/health`

Returns the health status of the API and its downstream components.
Designed for Kubernetes liveness/readiness probes and load-balancer health checks.

**Response `200 OK`:**
```json
{
  "status": "healthy",
  "version": "0.1.0",
  "environment": "development",
  "timestamp": "2025-01-01T00:00:00.000Z",
  "components": {
    "api": "healthy"
  }
}
```

---

## Environment Variables

Copy `backend/.env.example` to `backend/.env` and configure the values.

| Variable | Default | Description |
|----------|---------|-------------|
| `APP_NAME` | `Enterprise RAG AI Assistant` | Application display name |
| `APP_VERSION` | `0.1.0` | Semantic version string |
| `ENVIRONMENT` | `development` | `development` / `staging` / `production` |
| `DEBUG` | `false` | Enable debug mode (never `true` in prod) |
| `HOST` | `0.0.0.0` | Server bind address |
| `PORT` | `8000` | Server port |
| `WORKERS` | `1` | Uvicorn worker count |
| `API_V1_PREFIX` | `/api/v1` | URL prefix for v1 endpoints |
| `ALLOWED_ORIGINS` | `http://localhost:3000` | Comma-separated CORS origins |
| `LOG_LEVEL` | `INFO` | `DEBUG` / `INFO` / `WARNING` / `ERROR` |
| `LOG_FORMAT` | `text` | `text` (dev) or `json` (prod) |
| `SECRET_KEY` | *(placeholder)* | **MUST** be changed before production |

---

## Running Tests

```bash
cd backend

# Activate virtual environment first
source .venv/bin/activate   # or .venv\Scripts\activate on Windows

# Run the full test suite
pytest tests/ -v

# Run with coverage report
pytest tests/ -v --tb=short
```

Expected output:
```
tests/test_health.py::TestRootEndpoint::test_root_returns_200 PASSED
tests/test_health.py::TestRootEndpoint::test_root_returns_message PASSED
tests/test_health.py::TestRootEndpoint::test_root_content_type_is_json PASSED
tests/test_health.py::TestRootEndpoint::test_root_has_request_id_header PASSED
tests/test_health.py::TestHealthEndpoint::test_health_returns_200 PASSED
tests/test_health.py::TestHealthEndpoint::test_health_status_is_healthy PASSED
...
```

---

## Future Roadmap

| Phase | Description | Key Technologies |
|-------|-------------|-----------------|
| **Phase 1** ✅ | Project foundation, FastAPI skeleton, Next.js shell, Docker | FastAPI, Next.js, Docker |
| **Phase 2** | Authentication & authorisation | JWT, OAuth2, BCrypt |
| **Phase 3** | Database layer, caching | PostgreSQL, pgvector, SQLAlchemy, Redis |
| **Phase 4** | RAG pipeline, document ingestion, semantic search | LangChain, OpenAI, Embeddings |
| **Phase 5** | Production deployment, monitoring, CI/CD | Nginx, GitHub Actions, Prometheus |

---

## Contributing

1. Create a feature branch from `main`.
2. Follow the existing code style (Ruff for Python, ESLint for TypeScript).
3. Write tests for all new functionality.
4. Submit a pull request with a clear description of changes.

---

## License

MIT License — see [LICENSE](LICENSE) for details.
